%section.logo
  =link_to root_path do
    =image_tag('logo.svg', class: 'logo', title: 'Go home, brah')
%section.user-tools
  - unless current_user.nil?
    = render 'users/tools'
  - else
    %a.login.google
      %i.fa.fa-lg.fa-google-plus(title="Sign in")
    %a.login.github
      %i.fa.fa-lg.fa-github(title="Sign in")

-# - if current_user.nil?
-#   %p
-#     %span.header-text
-#       Log in to do stuff like saving your polls and seeing who votes in 'em.
-#   %a.login
-#     %i.fa.fa-rocket.shake{title: 'CLICK ME GOD DAMN IT'}
-# - else
-#   .current-user
-#     %i.fa.fa-google-plus-square.fa-2x
-#     %span= current_user.email
-#   = link_to logout_path, class: 'logout' do
-#     %i.fa.fa-unlock-alt.fa-2x{title: 'Log out'}
-#   = link_to user_polls_path(current_user) do
-#     %i.fa.fa-bar-chart-o.fa-2x{title: 'View your polls'}
-#   = link_to root_path do
-#     %i.fa.fa-pencil.fa-2x{title: 'New poll'}
-#   - if @poll && !@poll.new_record?
-#     - unless @poll.options.empty?
-#       %a.share{ data: { url: "poll.nplol.com/#{@poll.slug}"}}
-#         %i.fa.fa-bomb.fa-2x{title: 'Share this poll'}
-#     -if @poll.owner == current_user
-#       = link_to edit_poll_path(@poll) do
-#         %i.fa.fa-cut.fa-2x{title: 'Edit poll'}
-#       = link_to @poll, { method: :delete, class: 'delete-poll', data: { confirm: 'Are you sure?' } } do
-#         %i.fa.fa-trash-o.fa-2x{title: 'Delete poll'}
-#       - if @poll.closed?
-#         = link_to open_poll_path(@poll) do
-#           %i.fa.fa-magic.fa-2x{title: 'Open poll'}
-#       - else
-#         = link_to close_poll_path(@poll) do
-#           %i.fa.fa-gavel.fa-2x{title: 'Close poll'}
-#       = link_to @poll do
-#         %i.fa.fa-paper-plane-o.fa-2x{title: 'View poll'}
