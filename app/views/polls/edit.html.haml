%h1= @poll.name

%section
  %h2
    options
  = link_to new_poll_option_path(@poll),  id: 'add-option', remote: true do
    %i.fa.fa-plus.fa-2x{title: 'Add new option'}
  %section#options
    - @poll.options.reverse.each do |option|
      = render option

